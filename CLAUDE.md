# 株式会社アイシン (Aishin) コーポレートサイト

## ⚠️ 最重要ルール（厳守）

### 1. 不明点ゼロで作業する
**Webサイトを作成する上で不明点がある場合は必ずユーザーに質問し、不明点が一切ない状態で作業を進めること。**

- 仕様・デザイン・文言・構成などに少しでも曖昧な点があれば、推測で進めずに必ず質問する
- 質問への回答を得て、不明点がゼロになってから実装に着手する
- このルールはこのプロジェクトの全作業に適用される

### 2. 修正完了後は必ず GitHub Pages にデプロイする
**修正作業が終わったら毎回 `npm run deploy` で GitHub Pages にデプロイし、本番環境で確認できる状態にすること。**

- リポジトリ: https://github.com/jime0083/aishin（リモート名: `aishin`）
- 本番URL: https://jime0083.github.io/aishin/
- ソースコードも main ブランチにコミット・プッシュする

### 3. グリッド整列を徹底する
**サイト全体で要素の左端・右端のラインを揃えること。**

- 全セクションの見出し・本文・カード類は `.container` の左端ラインに揃える
- 意図なく左右のラインをずらすレイアウトは禁止（ずらす場合はデザイン意図を明確に）
- 新規セクション追加時・修正時は必ず隣接セクションとの縦ラインを確認する

### 4. 問題・タスクの記録
- 問題点・修正点は発生時に必ず `problem.txt` に記録し、修正後にステータスを更新する
- タスクは `progress.txt` に切り出して記録・管理する

### 5. タスクの粒度（progress.txt に追加する全タスクに適用）
**progress.txt に記録するタスクは、Opus や Sonnet クラスのモデルでも問題なく実装できる粒度・難易度に分解すること。**

- 1タスク = 対象ファイルが明確で、変更内容と完了条件が具体的に書かれている単位にする
- タスクには「対象ファイルパス」「何を作る/変えるか」「満たすべき仕様（コンポーネントのprops、CSSクラス名、データ構造等）」を明記する
- 「〜ページを作成」のような大きな単位のままにせず、データ定義／コンポーネント実装／スタイル／アニメーション／記録ファイル更新などに分割する
- 今後タスクを追加する際も必ずこの粒度を守る

### 6. 下層ページの開発フロー（1ページずつ確認）
**1つのページが完成したら必ず作業を止め、ユーザーの確認を待つこと。**

```
1ページ実装 → 型チェック・ビルド → デプロイ → ★作業停止★
→ ユーザーが本番URLで確認 → 修正箇所の共有
→ 修正 → デプロイ → 再確認 → 全ての修正をパス
→ 次のページの実装へ
```

- 複数ページをまとめて実装してはならない（1ページ完成ごとに必ず停止する）
- 修正指示は problem.txt に P番号で記録し、全てパスしてから次ページに着手する

## プロジェクト概要

- コンサル系ベンチャー企業「株式会社アイシン」のコーポレートサイト
- 採用ターゲット: 新卒〜35歳以下の若手・中堅人材
- 会社テーマ: 「まだ見ぬピースを発明する」(Inventing the Missing Piece) — ファーストビューで強調
- デプロイ先: GitHub Pages（リポジトリ名: aishin → https://<user>.github.io/aishin/）
- React SPA（HashRouter使用。GitHub Pagesで404にならないため）

## ページ構成

| ページ | パス | 状態 |
|--------|------|------|
| トップ（会社紹介） | `/` | ✅ 作成済み |
| サービス紹介 | `/service` | ✅ 作成済み（トップの3サービスを詳細化。トップのSERVICEセクションは削除済み） |
| 実績一覧 | `/works` | ✅ 作成済み（実績5件。件数の明示はしない） |
| 社員インタビュー | `/interview/:id` | ✅ 作成済み（3名分。一覧ページは廃止・トップのカードから個別詳細へ遷移。ナビにINTERVIEWリンクはない） |
| キャリアサポート | `/career` | ✅ 作成済み（キャリアサポート4項目＋支援制度10種） |
| 採用エントリーフォーム | `/entry` | ✅ 作成済み（送信はダミー動作。実送信は未接続） |

**共通仕様**: FVの落下パズルピース（WordPiece / WORD_PIECES）は全ページ共通。
画像はパズルピース型切り抜き（右カラム配置は puzzle-left で右ライン揃え）を基本とする。

## デザイン要件

- 物理演算UI × キネティック文字 × スクロール連動
- 配色: 白（ウォームホワイト）基調 × コーラルオレンジのアクセント × チャコールテキスト
  （参考: https://recruit.positive.co.jp/graduates/ ／ 黒基調は禁止）
- ヘッダーリンク・各ページ見出しは全て英語、本文は日本語
- FVコピーは日本語メイン「まだ見ぬピースを発明する」＋英語サブ "Inventing the Missing Piece"
- 画像はプレースホルダー枠のみ配置（`img.txt` に必要画像一覧を記載。ユーザーが今後用意する）
- ローディング速度を最優先（コード分割・フォントのsubset読み込み・軽量ライブラリ）

## コマンド

| コマンド | 用途 |
|----------|------|
| `npm run dev` | 開発サーバー起動 |
| `npm run build` | 型チェック＋本番ビルド |
| `npm run deploy` | GitHub Pagesへデプロイ（gh-pages） |

## 記録ファイル

- `progress.txt` — 進捗記録（最上部に使用ライブラリ一覧を記載・維持すること）
- `img.txt` — 配置予定画像の一覧（プレースホルダーIDと対応）
